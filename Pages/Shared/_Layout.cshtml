<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Caderno Vivo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
<div id="wrapper">

    <!-- Sidebar -->
    <nav id="sidebar">
        <div class="sidebar-brand">
            <i class="bi bi-journal-bookmark-fill"></i>
            <span>Caderno Vivo</span>
        </div>

        <ul class="nav flex-column mt-2">
            <li class="nav-item">
                <a class="nav-link nav-link-hoje" asp-page="/Hoje/Index">
                    <i class="bi bi-calendar-check-fill"></i> Hoje
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Index">
                    <i class="bi bi-speedometer2"></i> Dashboard
                </a>
            </li>

            <li class="nav-section">FACULDADE</li>

            <li class="nav-item">
                <a class="nav-link" asp-page="/Faculdade/Index">
                    <i class="bi bi-mortarboard-fill"></i> Matérias
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Faculdade/Tarefas/Index">
                    <i class="bi bi-check2-square"></i> Tarefas
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Faculdade/Duvidas/Index">
                    <i class="bi bi-question-circle"></i> Dúvidas
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Faculdade/Pendencias/Index">
                    <i class="bi bi-exclamation-triangle"></i> Pendências
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Faculdade/PlanoDaSemana/Index">
                    <i class="bi bi-calendar-week"></i> Plano da Semana
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Faculdade/Importar/Index">
                    <i class="bi bi-cloud-upload"></i> Importar JSON
                </a>
            </li>

            <li class="nav-section">ARTIGOS</li>

            <li class="nav-item">
                <a class="nav-link" asp-page="/Artigos/Index">
                    <i class="bi bi-journal-text"></i> Artigos
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Artigos/Tarefas/Index">
                    <i class="bi bi-check2-square"></i> Tarefas
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Artigos/Importar/Index">
                    <i class="bi bi-cloud-upload"></i> Importar JSON
                </a>
            </li>

            <li class="nav-section">FERRAMENTAS</li>

            <li class="nav-item">
                <a class="nav-link" asp-page="/PlanoImportar/Index">
                    <i class="bi bi-cloud-upload"></i> Importar Plano
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-page="/Exportar/Index">
                    <i class="bi bi-box-arrow-up"></i> Exportar
                </a>
            </li>
        </ul>
    </nav>

    <!-- Content -->
    <div id="content-wrapper">
        <!-- Topbar -->
        <div class="topbar">
            <button id="sidebarToggle" class="btn btn-sm btn-outline-secondary">
                <i class="bi bi-list fs-5"></i>
            </button>
            <nav aria-label="breadcrumb" class="ms-2">
                <ol class="breadcrumb mb-0 small">
                    @RenderSection("Breadcrumb", required: false)
                </ol>
            </nav>
        </div>

        <!-- Page content -->
        <main class="main-content">
            @if (TempData["Sucesso"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show mb-3" role="alert">
                    <i class="bi bi-check-circle me-2"></i>@TempData["Sucesso"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }
            @if (TempData["Erro"] != null)
            {
                <div class="alert alert-danger alert-dismissible fade show mb-3" role="alert">
                    <i class="bi bi-exclamation-triangle me-2"></i>@TempData["Erro"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            }
            @RenderBody()
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
<script>
    document.getElementById('sidebarToggle').addEventListener('click', function () {
        document.getElementById('sidebar').classList.toggle('collapsed');
        document.getElementById('content-wrapper').classList.toggle('expanded');
    });

    // Marca link ativo na sidebar
    (function () {
        const path = window.location.pathname.toLowerCase();
        document.querySelectorAll('#sidebar .nav-link').forEach(link => {
            const href = link.getAttribute('href')?.toLowerCase();
            if (href && href !== '/' && path.startsWith(href)) {
                link.classList.add('active');
            } else if (href === '/' && path === '/') {
                link.classList.add('active');
            }
        });
    })();
</script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>
